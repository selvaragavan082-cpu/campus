# Frontend Environment Variables
# Leave empty in local development to use Vite's built-in proxy (/api -> localhost:5000)
# Set to your full backend URL in production (e.g. https://your-backend-api.onrender.com/api)
VITE_API_URL=/api
